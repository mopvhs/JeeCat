<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.cat.dao.MaocheAlimamaUnionTitleKeywordDao">
	
	<!-- 查询数据
	<select id="findList" resultType="MaocheAlimamaUnionTitleKeywordDO">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<update id="updateTag">
		update
		maoche_alimama_union_title_keyword
		<set>
			content_manual = #{contentManual}
		</set>
		<where>
			id = #{id}
		</where>
	</update>

</mapper>